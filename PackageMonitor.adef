start: manual
sandboxed: true
version: 0.1

bundles: {
    dir: {
        //load the log files directory
        [w] dataComponent/logfiles /logfiles
    }
    file: {
        //load the config file into the system etc directory
        [r] dataComponent/package-monitor-configfile /etc/
    }
}

executables: {
    LogTest = ( dataComponent )
}

processes: {
    run: {
        ( LogTest )
    }
    envVars: {
        LE_LOG_LEVEL = DEBUG
    }
}
